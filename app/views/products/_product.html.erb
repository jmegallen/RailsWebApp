<div class="col-sm-2 col-md-4 prodtile">
  <h3><%= product.name %></h3><%= image_tag(product.image_url, class: "img-prod img-responsive center-block") %>
  <h4><strong>$</strong><%= product.price %></h4>
  <h4><%= product.colour %></h4>
  <p><%= product.description %></p>
  <div aria-label="..." class="btn-group-sm" role="group">
    <button class="btn prodbut btn-default" type="button"><%= link_to 'Show', product %></button> 
<button class="btn prodbut btn-default" type="button"><%= link_to 'Edit', edit_product_path(product) %></button> 
<button class="btn prodbut btn-default" type="button"><%= link_to 'Delete', product, method: :delete, data: { confirm: 'Are you sure?' } %></button>
  </div>
</div>