<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, class:"col-2 col-form-label" %>
    <%= f.text_field :name class:"form-control", type:"text", placeholder:"Name", id:"example-text-input" %>
  </div>

  <div class="field">
    <%= f.label :email, class:"col-2 col-form-label"%>
    <%= f.text_field :email class:"form-control", type:"text", placeholder:"Email", id:"example-text-input" %>
  </div>

  <div class="actions">
    <%= f.submit class:"btn btn-default" %>
  </div>
<% end %>
